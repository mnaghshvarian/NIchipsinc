<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>NI6GNET — AI Atlas Inc.</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{--gold:#ffd166;--gold2:#ffe6a3;--cyan:#34d3ee;--text:#eef4ff;--mut:#bcccea;}
  *{box-sizing:border-box;margin:0;padding:0;}
  html,body{height:100%;background:#04060f;overflow:hidden;font-family:'Inter','Segoe UI',Arial,sans-serif;}
  .wrap{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;}
  .frame{position:absolute;top:50%;left:50%;width:1080px;height:1350px;transform-origin:center center;
         border-radius:24px;overflow:hidden;background:linear-gradient(180deg,#0e2350,#0a1838);
         box-shadow:0 50px 150px rgba(52,211,238,.22),0 18px 54px rgba(0,0,0,.7);}
  .head{position:absolute;top:0;left:0;right:0;height:250px;background:linear-gradient(180deg,#11295c,#0c1c43);z-index:5;}
  .badge{position:absolute;top:38px;left:48px;display:flex;align-items:center;gap:9px;
         border:1.5px solid var(--gold);border-radius:999px;padding:7px 16px;}
  .badge i{width:8px;height:8px;border-radius:50%;background:#3df0ad;box-shadow:0 0 12px #3df0ad;animation:blink 2s infinite;}
  @keyframes blink{0%,100%{opacity:1}50%{opacity:.35}}
  .badge span{color:var(--gold);font-size:16px;letter-spacing:3px;font-weight:700;}
  .atlas{position:absolute;top:40px;right:48px;color:#fff;font-family:'Montserrat',sans-serif;font-weight:700;font-size:25px;}
  .word{position:absolute;top:86px;left:0;right:0;text-align:center;font-family:'Montserrat',sans-serif;
        font-weight:800;font-size:116px;letter-spacing:4px;line-height:1;
        background:linear-gradient(96deg,#ffe6a3 0%,#ffffff 40%,#34d3ee 72%,#ffe6a3 100%);
        background-size:260% 100%;-webkit-background-clip:text;background-clip:text;color:transparent;
        filter:drop-shadow(0 0 30px rgba(52,211,238,.45));animation:shimmer 6s linear infinite;}
  @keyframes shimmer{to{background-position:260% 0}}
  .tag{position:absolute;top:198px;left:0;right:0;text-align:center;color:#dce8ff;font-size:25px;font-weight:500;}
  canvas#scene{position:absolute;top:250px;left:0;width:1080px;height:806px;display:block;}
  .labels{position:absolute;top:250px;left:0;width:1080px;height:806px;pointer-events:none;z-index:3;}
  .lab{position:absolute;transform:translate(-50%,-50%);display:flex;align-items:center;gap:6px;
       background:rgba(8,18,42,.78);border:1px solid rgba(120,150,200,.5);border-radius:999px;
       padding:4px 11px;font-size:14px;font-weight:600;color:#eaf2ff;white-space:nowrap;}
  .lab i{width:7px;height:7px;border-radius:50%;}
  .lab.u i{background:var(--gold);} .lab.c i{background:var(--cyan);} .lab.h i{background:#fff;}
  .cap{position:absolute;top:960px;left:0;right:0;text-align:center;z-index:4;}
  .cap span{display:inline-block;background:rgba(8,18,42,.7);border:1px solid rgba(52,211,238,.35);
       border-radius:12px;padding:10px 22px;color:#eaf4ff;font-size:24px;font-weight:600;
       opacity:0;transition:opacity .5s ease;}
  .cap span.on{opacity:1;}
  .foot{position:absolute;top:1056px;left:0;right:0;height:294px;background:linear-gradient(0deg,#11295c,#0c1c43);z-index:5;}
  .data{position:absolute;top:24px;left:0;right:0;text-align:center;}
  .pill{display:inline-flex;align-items:center;gap:13px;background:rgba(14,30,64,.9);
        border:1.5px solid #3a63a0;border-radius:999px;padding:11px 26px;}
  .pill .lbl{color:var(--mut);font-size:17px;letter-spacing:1px;}
  .pill .per{color:var(--cyan);font-weight:800;font-size:29px;font-family:'Montserrat',sans-serif;min-width:74px;text-align:center;}
  .pill .agg{color:var(--gold);font-weight:800;font-size:29px;font-family:'Montserrat',sans-serif;min-width:128px;text-align:center;}
  .cta{position:absolute;top:102px;left:0;right:0;text-align:center;}
  .cta a{display:inline-block;background:linear-gradient(96deg,#ffd166,#ffe6a3);color:#0a1430;
         font-family:'Montserrat',sans-serif;font-weight:800;font-size:25px;
         padding:15px 42px;border-radius:999px;text-decoration:none;box-shadow:0 12px 36px rgba(255,209,102,.42);}
  .contact{position:absolute;top:180px;left:0;right:0;text-align:center;color:#eef4ff;font-size:22px;font-weight:600;}
  .contact b{color:var(--gold);}
  .ni{position:absolute;top:234px;left:0;right:0;text-align:center;color:#9fb2d8;font-size:14px;letter-spacing:2px;}
  .hint{position:fixed;bottom:10px;left:0;right:0;text-align:center;color:#56688c;font-size:12px;}
</style>
</head>
<body>
<div class="wrap">
  <div class="frame" id="frame">
    <canvas id="scene"></canvas>
    <div class="labels" id="labels"></div>
    <div class="cap"><span id="cap"></span></div>
    <div class="head">
      <div class="badge"><i></i><span>NOW ANNOUNCING</span></div>
      <div class="atlas">AI&nbsp;Atlas&nbsp;Inc.</div>
      <div class="word">NI6GNET</div>
      <div class="tag">Fibre-grade connectivity — wireless, self-aligning, and it follows you.</div>
    </div>
    <div class="foot">
      <div class="data"><div class="pill">
        <span class="lbl">PER&nbsp;USER</span><span class="per" id="per">10G</span>
        <span class="lbl">AGGREGATE</span><span class="agg" id="agg">1&nbsp;Tbps</span>
      </div></div>
      <div class="cta"><a href="https://www.aiatlasinc.ca">Reserve a pilot slot →</a></div>
      <div class="contact"><b>www.aiatlasinc.ca</b>&nbsp;&nbsp;·&nbsp;&nbsp;info@aiatlasinc.ca</div>
      <div class="ni">© AI ATLAS INC. · NI6GNET™ · POWERED BY NATURAL INTELLIGENCE</div>
    </div>
  </div>
</div>
<div class="hint">Live 3D — screen-record this card (~16 s) and upload as a LinkedIn video.</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
<script>
const FW=1080,FH=1350,CW=1080,CH=806,CTOP=250;
const frame=document.getElementById('frame');
function fit(){const s=Math.min(innerWidth/FW,innerHeight/FH)*0.97;frame.style.transform=`translate(-50%,-50%) scale(${s})`;}
addEventListener('resize',fit);fit();

const canvas=document.getElementById('scene');
const renderer=new THREE.WebGLRenderer({canvas,antialias:true});
renderer.setPixelRatio(Math.min(devicePixelRatio,2));
renderer.setSize(CW,CH,false);

const scene=new THREE.Scene();
scene.background=new THREE.Color(0x0e2147);
scene.fog=new THREE.FogExp2(0x0e2147,0.0085);
const camera=new THREE.PerspectiveCamera(40,CW/CH,0.1,500);

scene.add(new THREE.AmbientLight(0x9fb4dc,1.05));
const dl=new THREE.DirectionalLight(0xffffff,0.8);dl.position.set(14,40,24);scene.add(dl);
const pg=new THREE.PointLight(0xffd166,1.3,170);pg.position.set(0,22,-2);scene.add(pg);
const pc=new THREE.PointLight(0x34d3ee,1.0,170);pc.position.set(-14,18,6);scene.add(pc);

function glowTex(){const c=document.createElement('canvas');c.width=c.height=128;const x=c.getContext('2d');
  const g=x.createRadialGradient(64,64,0,64,64,64);
  g.addColorStop(0,'rgba(255,255,255,1)');g.addColorStop(.3,'rgba(255,255,255,.6)');g.addColorStop(1,'rgba(255,255,255,0)');
  x.fillStyle=g;x.fillRect(0,0,128,128);return new THREE.CanvasTexture(c);}
const GLOW=glowTex();
function halo(color,scale,op){const s=new THREE.Sprite(new THREE.SpriteMaterial({map:GLOW,color,blending:THREE.AdditiveBlending,transparent:true,opacity:op||1,depthWrite:false}));s.scale.set(scale,scale,1);return s;}

// stars
(()=>{const g=new THREE.BufferGeometry();const n=180;const p=new Float32Array(n*3);
  for(let i=0;i<n;i++){p[i*3]=(Math.random()-0.5)*220;p[i*3+1]=20+Math.random()*60;p[i*3+2]=-20-Math.random()*140;}
  g.setAttribute('position',new THREE.BufferAttribute(p,3));
  scene.add(new THREE.Points(g,new THREE.PointsMaterial({color:0xbfd2f5,size:0.6,transparent:true,opacity:.8})));})();

const grid=new THREE.GridHelper(220,44,0x2a4a78,0x18304f);grid.position.y=0;scene.add(grid);
const hz=halo(0xffd166,110,0.22);hz.position.set(0,3,-58);scene.add(hz);

const up=new THREE.Vector3(0,1,0);
function setBeam(bm,a,b){bm.a.copy(a);bm.b.copy(b);
  const dir=new THREE.Vector3().subVectors(b,a);const len=dir.length();
  bm.mesh.position.copy(a).add(b).multiplyScalar(.5);bm.mesh.scale.set(1,len,1);
  if(len>1e-4)bm.mesh.quaternion.setFromUnitVectors(up,dir.clone().normalize());}
function beam(a,b,color,radius,op){
  const geo=new THREE.CylinderGeometry(radius,radius,1,12,1,true);
  const mat=new THREE.MeshBasicMaterial({color,transparent:true,opacity:op,blending:THREE.AdditiveBlending,depthWrite:false});
  const m=new THREE.Mesh(geo,mat);scene.add(m);
  const pulse=halo(color,2.8);scene.add(pulse);
  const bm={mesh:m,a:a.clone(),b:b.clone(),pulse,t:Math.random()};setBeam(bm,a,b);return bm;}

// homes (users)
const homePts=[new THREE.Vector3(-15,0,-3),new THREE.Vector3(-4,0,-12),new THREE.Vector3(10,0,-5),new THREE.Vector3(17,0,-16)];
const homes=homePts.map((p,i)=>{
  const m=new THREE.Mesh(new THREE.CylinderGeometry(0.5,0.75,1.4,6),new THREE.MeshStandardMaterial({color:0x33486e,emissive:0xffd166,emissiveIntensity:.8}));
  m.position.copy(p).setY(0.7);scene.add(m);
  const h=halo(0xffd166,5,.95);h.position.copy(p).setY(0.7);scene.add(h);
  m.userData={h,ph:i*1.3};return m;});

// aerostat (NI6GNET head)
const aero=new THREE.Group();
const balloon=new THREE.Mesh(new THREE.SphereGeometry(5,30,26),new THREE.MeshStandardMaterial({color:0x213a64,emissive:0x16304f,metalness:.3,roughness:.4}));
balloon.scale.set(1,1.22,1);balloon.position.y=2;aero.add(balloon);
const ring=new THREE.Mesh(new THREE.TorusGeometry(5.05,0.13,10,40),new THREE.MeshStandardMaterial({color:0xffd166,emissive:0xffd166,emissiveIntensity:.9}));
ring.rotation.x=Math.PI/2;ring.position.y=2;aero.add(ring);
const pod=new THREE.Mesh(new THREE.BoxGeometry(3.2,1.4,2.4),new THREE.MeshStandardMaterial({color:0x1a2e52,emissive:0x3a2c10,emissiveIntensity:.5}));
pod.position.y=-4.4;aero.add(pod);
const lens=new THREE.Mesh(new THREE.SphereGeometry(0.8,16,16),new THREE.MeshStandardMaterial({color:0xffe6a3,emissive:0xffd166,emissiveIntensity:1.8}));
lens.position.y=-5.3;aero.add(lens);
aero.add(halo(0xffd166,16,.85));
aero.position.set(0,21,-6);scene.add(aero);
const podW=new THREE.Vector3(0,21-5.3,-6);

// drone
const drone=new THREE.Group();
drone.add(new THREE.Mesh(new THREE.BoxGeometry(2,0.7,2),new THREE.MeshStandardMaterial({color:0x1c2f4e,emissive:0x123050,emissiveIntensity:.6})));
[[-1.4,-1.4],[1.4,-1.4],[-1.4,1.4],[1.4,1.4]].forEach(a=>{const r=new THREE.Mesh(new THREE.CylinderGeometry(0.8,0.8,0.08,16),new THREE.MeshStandardMaterial({color:0x1c4a72,emissive:0x1a5c8e,emissiveIntensity:.7,transparent:true,opacity:.75}));r.position.set(a[0],0.4,a[1]);drone.add(r);r.userData.spin=true;});
drone.add(halo(0x34d3ee,5,.8));
drone.position.set(-19,15,4);scene.add(drone);
const droneW=new THREE.Vector3(-19,15,4);

// LEO satellite (clearly in open sky)
const leo=new THREE.Group();
leo.add(new THREE.Mesh(new THREE.BoxGeometry(2.4,2,2.4),new THREE.MeshStandardMaterial({color:0x223a60,emissive:0x163a5e,emissiveIntensity:.7})));
[-3.2,3.2].forEach(x=>{const pn=new THREE.Mesh(new THREE.BoxGeometry(3.2,0.1,1.7),new THREE.MeshStandardMaterial({color:0x1c4a72,emissive:0x1a6da0,emissiveIntensity:.8}));pn.position.x=x;leo.add(pn);});
leo.add(halo(0x34d3ee,7,.9));
leo.position.set(0,28,-22);scene.add(leo);
const leoW=new THREE.Vector3(0,28,-22);

// vehicle (mobile user)
const car=new THREE.Group();
car.add(new THREE.Mesh(new THREE.BoxGeometry(2.2,0.7,1.1),new THREE.MeshStandardMaterial({color:0x2a3f5e,emissive:0x0e1a30,emissiveIntensity:.4})));
car.add(new THREE.Mesh(new THREE.BoxGeometry(1.2,0.6,1),new THREE.MeshStandardMaterial({color:0x34507a})));
car.add(halo(0x34d3ee,4.2,.9));
car.position.set(0,0.7,9);scene.add(car);
const carW=new THREE.Vector3(0,1.0,9);

// beams
const beams=[];
homePts.forEach(p=>beams.push(beam(podW,p,0xffd166,0.14,.5)));
const trackBeam=beam(podW,carW,0x34d3ee,0.18,.85);beams.push(trackBeam);
beams.push(beam(droneW,homePts[0],0xffd166,0.12,.4));
const backhaul=beam(leoW,new THREE.Vector3(0,26,-6),0x34d3ee,0.11,.6);beams.push(backhaul);

// labels
const labelsDiv=document.getElementById('labels');
function mkLab(text,cls){const e=document.createElement('div');e.className='lab '+cls;e.innerHTML='<i></i>'+text;labelsDiv.appendChild(e);return e;}
const labelDefs=[
  {el:mkLab('NI6GNET head','h'),get:()=>new THREE.Vector3(0,aero.position.y+6,-6)},
  {el:mkLab('LEO backhaul','c'),get:()=>leoW.clone().setY(31)},
  {el:mkLab('Drone relay','c'),get:()=>droneW.clone().setY(drone.position.y+2.4)},
  {el:mkLab('User','u'),get:()=>homePts[0].clone().setY(2.4)},
  {el:mkLab('User','u'),get:()=>homePts[1].clone().setY(2.4)},
  {el:mkLab('User','u'),get:()=>homePts[2].clone().setY(2.4)},
  {el:mkLab('User','u'),get:()=>homePts[3].clone().setY(2.4)},
  {el:mkLab('Mobile user — beam follows','c'),get:()=>carW.clone().setY(2.4)}
];
const vtmp=new THREE.Vector3();
function projLabels(){
  labelDefs.forEach(d=>{
    vtmp.copy(d.get()).project(camera);
    if(vtmp.z>1){d.el.style.display='none';return;}
    d.el.style.display='flex';
    let x=(vtmp.x*0.5+0.5)*CW;
    let y=(1-(vtmp.y*0.5+0.5))*CH;
    x=Math.max(70,Math.min(CW-70,x));
    y=Math.max(24,Math.min(CH-150,y));
    d.el.style.left=x+'px';d.el.style.top=y+'px';
  });
}

// captions (baked description)
const caps=[
  "One head — up to 100 fibre-grade beams",
  "Every user gets its own dedicated beam",
  "Users move, the beam follows — automatic handover",
  "Backhaul by LEO satellite, aerostat or drone",
  "Self-aligning · no moving parts · powered by NI"
];
const capEl=document.getElementById('cap');let ci=-1;
function nextCap(){capEl.classList.remove('on');
  setTimeout(()=>{ci=(ci+1)%caps.length;capEl.textContent=caps[ci];capEl.classList.add('on');},400);}
nextCap();setInterval(nextCap,3400);

// data tiers
const tiers=[["1G","100 Gbps"],["10G","1 Tbps"],["25G","2.5 Tbps"],["100G","10 Tbps"],["400G","40 Tbps"]];
let tiI=1;const perEl=document.getElementById('per'),aggEl=document.getElementById('agg');
function showTier(){perEl.textContent=tiers[tiI][0];aggEl.innerHTML=tiers[tiI][1].replace(' ','&nbsp;');}
showTier();setInterval(()=>{tiI=(tiI+1)%tiers.length;showTier();},2400);

const targ=new THREE.Vector3(0,11,-6);
const t0=performance.now();
function loop(){
  requestAnimationFrame(loop);
  const t=(performance.now()-t0)/1000;
  const az=Math.sin(t*0.09)*0.14;
  camera.position.set(Math.sin(az)*56,24+Math.sin(t*0.07)*1.4,-6+Math.cos(az)*56);
  camera.lookAt(targ);

  aero.position.y=21+Math.sin(t*0.7)*0.9;podW.set(0,aero.position.y-5.3,-6);
  drone.position.y=15+Math.sin(t*1.1+1)*0.7;droneW.set(-19,drone.position.y,4);
  drone.children.forEach(c=>{if(c.userData.spin)c.rotation.y+=0.35;});
  leo.position.x=Math.sin(t*0.16)*7;leoW.set(leo.position.x,28,-22);
  const vx=Math.sin(t*0.22)*15;car.position.set(vx,0.7,9);carW.set(vx,1.0,9);

  homePts.forEach((p,i)=>setBeam(beams[i],podW,p));
  setBeam(trackBeam,podW,carW);
  setBeam(beams[homePts.length+1],droneW,homePts[0]);
  setBeam(backhaul,leoW,new THREE.Vector3(0,aero.position.y+5,-6));
  beams.forEach(bm=>{bm.t=(bm.t+0.01)%1;bm.pulse.position.lerpVectors(bm.a,bm.b,bm.t);});

  homes.forEach(h=>{const k=0.7+Math.sin(t*1.6+h.userData.ph)*0.3;h.material.emissiveIntensity=k;h.userData.h.material.opacity=0.6+k*0.35;});

  renderer.render(scene,camera);
  projLabels();
}
loop();
</script>
</body>
</html>
